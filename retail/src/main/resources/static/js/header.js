//const searchInput = document.getElementById("txtSearch").value; // Lấy giá trị tìm kiếm từ ô input
//const encodedSearchInput = encodeURIComponent(searchInput).replace(/\+/g, '%20'); // Thay thế dấu cộng bằng %20
//const url = `http://localhost:8090/search_ml?txtSearch=${encodedSearchInput}`;
//window.location.href = url;